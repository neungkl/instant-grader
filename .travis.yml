language: cpp

os:
  - linux
  - osx

matrix:
  include:
    - os: linux
      dist: trusty
    - os: osx
      osx_image: xcode8

addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - gcc-4.8
    - g++-4.8

script:
  - make build
  - make test
