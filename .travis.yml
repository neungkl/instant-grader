language: cpp

os:
  - linux

matrix:
  include:
    - os: linux
      dist: trusty

addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - gcc-4.8
    - g++-4.8

script:
  - make build
  - make test
