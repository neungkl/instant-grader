dist: trusty

before_install:
  - sudo add-apt-repository -y ppa:ubuntu-toolchain-r/test

install:
  - sudo apt-get install -qq g++-5

before_script:
  - g++ --version

script:
  - make build
  - make
  - make test
